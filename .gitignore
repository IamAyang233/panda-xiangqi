# 本地构建产物（由 `make build` / `make cross` 生成，勿提交）
/panda-xiangqi
/panda-xiangqi.exe
/panda-xiangqi-windows-amd64.exe
/qi-jing
/qi-jing.exe
/qi-jing.exe~
dist/

# 运行时日志
server.log

# 本地配置（保留 config.yaml.example）
config.yaml

# 飞牛 fnOS 打包产物（由 `make fpk` / `make fpk-arm` 重新生成，勿提交）
fnpack.exe
*.fpk
fnos/panda-xiangqi/app/server/panda-xiangqi
fnos/panda-xiangqi/app/server/engines/
fnos/panda-xiangqi-arm/

# 自定义残局默认目录（config 项 custom / QIJING_CUSTOM 的默认值 ./custom-puzzles，
# 在仓库里跑服务时会写到这里，属用户数据、勿入库）。注意别加尾斜杠以外的变体。
/custom-puzzles/

# 内置皮卡鱼引擎（官方 release 二进制 + NNUE 权重，体量大，单独放置、勿入库）
dist-engines/

# 临时下载 / 解包目录（7z 与解压产物，勿入库）
.tmp_engines/

# 图标草稿与一次性生成脚本（非源码）
icon-drafts/
_logo_*.py
icon_*_512.png

# 诊断/临时脚本与一次性产物（开发调试用，勿入库）
.diag/
_tmp_*.js
_check_palace.py
test_shot_*.png
panda-xiangqi~
matecheck.exe
/bin/

# 带版本号发布包与一次性诊断产物（勿入库）
panda-xiangqi_*.exe
panda-xiangqi_*.exe~
*.zip
.audit_*.mjs
.diag_check_palace.mjs
TEST_REPORT_*.md

# 本地测试环境指引（含内网 IP / 代理地址，勿入库）
TEST_VM_GUIDE.md

# 旧题库备份（已被 canju12 3577 题替换，勿入库）
internal/_old_puzzles_backup/

# 本地开发用的引擎工作目录（含 53MB NNUE 权重，勿提交）
/engines/

# Go 编译与测试产物（本地生成，勿提交）
/*.exe
/*.test.exe
